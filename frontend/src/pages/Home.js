import TicketList from "../components/ticketlist/TicketList";

const Home = () => {
    return (
        <div className="home">
            <TicketList/>
        </div>
    );
};

export default Home;